import type { PageActionTypes } from "types";

type AcaoProps = {
  pageActionType: PageActionTypes;
};

export const Acao = ({ pageActionType }: AcaoProps): JSX.Element => {
  return (
    <div>
      <h1></h1>
    </div>
  );
};
